# Plan documents

This directory is historical implementation context, not current operator guidance.

Use the repository root `README.md` and `doc/telegram-bot-features.md` for current cc-telegram setup, migration, hook, and command behavior. Files here are dated plans, external-review briefs, trackers, and remediation notes; older naming such as `ccbot` may appear as migration/history context and should not be read as the current runtime command or config directory.
